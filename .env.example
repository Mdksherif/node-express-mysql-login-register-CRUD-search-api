# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=your_mysql_password
DB_NAME=node_mysql_crud
PRODUCT_DB_NAME=productsdb

# JWT Configuration
JWT_SECRET=your_generated_jwt_secret_here_32_characters_minimum

# Server Configuration
PORT=3000
NODE_ENV=development

# Google Cloud Storage Configuration
GOOGLE_CLOUD_PROJECT_ID=your-gcs-project-id
GOOGLE_CLOUD_BUCKET_NAME=your-gcs-bucket-name
GOOGLE_CLOUD_KEY_FILE=./config/google-cloud-key.json
STORAGE_TYPE=local

# Storage Options:
# STORAGE_TYPE=local   (for local file storage)
# STORAGE_TYPE=gcs     (for Google Cloud Storage)
